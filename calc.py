x=input("What's x? ")
y=input("what's y? ")

print(x + y)

# What's happening here, is that the '+' op is concatenating(joining) strings instead of adding no.s
# But we typed a no. not a string
# By default, any input received from the keyboard, is a str; that's why.